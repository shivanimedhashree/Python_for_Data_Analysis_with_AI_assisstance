# -*- coding: utf-8 -*-
"""SHIVANI_MEDHASHREE_Airline_flights_dataset.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1ukZzs6sZNkbZ6_aJFfMVKpQJz3-L8U1L

# DAY 1

**Task:** Load Dataset Airline and generate first five rows and last five rows
"""

# Import required libraries

import numpy as np
import pandas as pd

print("Numpy imported successfully")
print("Pandas imported successfully")

import os
os.getcwd()

#Loading Dataset
df_airline = pd.read_csv("/content/airlines_flights_data.csv")

print("Dataset loaded successfully")

#Display first rows

df_airline.head(5)

#Display last 5 rows

df_airline.tail(5)

"""# DAY 2

**Task** - Load Dataset Airline and apply all the pandas operations
"""

#Loading Dataset
df_airline = pd.read_csv("/content/airlines_flights_data.csv")

print("Dataset loaded successfully")

#code1
df_airline.head()

#code2
df_airline.tail()

#code3
df_airline.shape

#code4
df_airline.columns

#code5
df_airline.dtypes

#code6
df_airline.info()

#code7
df_airline.describe()

#code8
df_airline["airline"]

#code9
df_airline[["airline", "flight"]]

#code10
df_airline.loc[:, :]

#code11
df_airline.iloc[:, :]

#code12
df_airline[df_airline["price"] > 10000]

#code13
df_airline["airline"].unique()

#code14
df_airline["airline"].value_counts()

#code15
df_airline["price"].sum()

#code16
df_airline["price"].mean()

#code17
df_airline["price"].max()

#code18
df_airline["price"].min()

"""# DAY 3"""

#Loading Dataset
df_airline = pd.read_csv("/content/airlines_flights_data.csv")

print("Dataset loaded successfully")

"""**Task 1:** Inspect the airline dataset and display
- First 10 rows
- Last 10 rows
- Number of rows and columns
- All the column names
"""

#code
display(df_airline.head(10))

display(df_airline.tail(10))

print("Shape:", df_airline.shape)

print("Columns:")
print(df_airline.columns.tolist())

"""**Task 2:** Select important flight columns and display first 10 records names of the columns namely,
- airline
- source_city
- destination_city
- class
- duration
- price.
"""

#code
df_airline[
    ["airline", "source_city", "destination_city", "class", "duration", "price"]
].head(10)

"""**Task 3:** Find expensive flights, where price is greater than 20000. Display the 10 records."""

#code
expensive_flights = df_airline[df_airline["price"] > 20000]

print("Number of Flights:", len(expensive_flights))

display(expensive_flights.head(10))

"""**Task 4:** Filter by airline. Find flights operated uniquely."""

#code
flights_by_airline = df_airline.groupby('airline')['flight'].unique()

print(flights_by_airline)

"""**Task 5:** Filter by class. Find flights belonging to the economy class"""

#code
economy_flights = df_airline[df_airline['class'] == 'Economy']

print(economy_flights)

"""**Task 6:** Multiple Conditions.
Find flights where price is greater than 50000 and duration is greater than 10 hours.
"""

#code
result = df_airline[
    (df_airline["price"] > 50000) &
    (df_airline["duration"] > 10)
]
display(result.head())

"""**Task 7:** Airline distribution. Find
- Unique airlines
- Number of airlines
- Number of flights for each airline
"""

#code

print("airline:")
print(df_airline["airline"].unique())

print("\nNumber of airline:")
print(df_airline["airline"].nunique())

print("\nNumber of flights for each airline:")
print(df_airline["airline"].value_counts())

"""**Task 8:** Understand Source_city. Find:
- Unique Source_city
- Number of Source_city
- Number of Source_city for each airline
"""

#code
print("Soure_city:")
print(df_airline["source_city"].unique())

print("\nNumber of Srce_city:")
print(df_airline["source_city"].nunique())

print("\nNumber of Source_city for each airline:")
print(df_airline["source_city"].value_counts())

"""**Task 9:** Price Analysis. Calculate:
- Total price
- Average price
- Median price
- Maximum price
- Minimum price
"""

#code
print("Total price:", df_airline["price"].sum())
print("Average price:", df_airline["price"].mean())
print("Median price:", df_airline["price"].median())
print("Maximum price:", df_airline["price"].max())
print("Minimum price:", df_airline["price"].min())

"""**Task 10:** Duration Analysis. Calculate:
- Total duration
- Average duration
- Median duration
- Maximum duration
- Minimum duration
"""

#code
print("Total duration:", df_airline["duration"].sum())
print("Average duration:", df_airline["duration"].mean())
print("Median duration:", df_airline["duration"].median())
print("Maximum duration:", df_airline["duration"].max())
print("Minimum duration:", df_airline["duration"].min())

"""# DAY 4 & DAY 5"""

#Loading Dataset
df_airline = pd.read_csv("/content/airlines_flights_data.csv")

print("Dataset loaded successfully")

"""**Task 1:** Single-Column Groupby Aggregation

**Question:** Calculate the average departure delay for each airline carriers.
"""

#code
airline_col = 'airline'
dep_col = 'departure_time'
origin_col = 'source_city'
dest_col = 'destination_city'

task1_airline = df_airline.groupby(airline_col)[dep_col].count().reset_index(name='Flight_Count')

print(task1_airline.head(5))

"""**Task 2:** Multi-Column Groupby Aggregation

**Question:** Calculate total flights and average departure delays grouped by origin and destination airports.
"""

#code
task2_airline = df_airline.groupby([origin_col, dest_col]).agg(
Flight_Count=(dep_col, 'count')
).reset_index()
print(task2_airline.head(5))

"""**Task 3:** Multi-Metric Aggregation with agg()

**Question:** Compute total flight count, mean depature delay and maximum departure delay per airline.
"""

#code
task3_airline = df_airline.groupby(airline_col).agg(
   Total_Flight_Count=(dep_col, 'count'),
).reset_index()
print(task3_airline.head(5))

"""**Task 4:** Sorting Aggregated Summaries

**Question:** Identify the top 5 Origin Airports handling the highest volume of departing flights.
"""

#code
task4_airline = (df_airline.groupby(origin_col)
                .size()
                .reset_index(name='Total_Flights')
                .sort_values(by='Total_Flights', ascending=False)
                .head(5))
print(task4_airline)

"""**Task 5:** Named Aggregations

**Question:** Compute total flight count, average departure delay and average arrival delay per airline.
"""

#code
arr_col = 'arrival_time'
task5_airline = (df_airline.groupby(airline_col).agg(
                    Total_Flights=(dep_col, 'count')
).reset_index())
print(task5_airline.head(5))

"""**Task 6:** Conditional Group Filtering\

**Question:** Filter the dataset to re-evaluate flights originating only from hub airports that handle over 10000 departing flights.
"""

#code
task6_airline = df_airline.groupby(origin_col).filter(lambda x: x[dep_col].count() > 10000)
print("Qualified Origin Airports:")
print(task6_airline[origin_col].unique())
print("Hub Flights Retained:", len(task6_airline))

"""**Task 7:** Derived Aggregate Metrics

**Question:** Calculate total flights, delayed flights (DEP_DELAY > 15), and Delayed Rate percentage per airline.
"""

#code
task7_airline = df_airline.groupby(airline_col).agg(
    Total_Flights=(dep_col, 'count')
).reset_index()
print(task7_airline.sort_values(by='Total_Flights', ascending=False).head(5))

"""**Task 8:** Extremum Selection with nlargest()

**Question:** Retrive the top 5 individual flight instances recording the worst overall departure delay.
"""

#code
task8_airline = df_airline.groupby(airline_col).size().nlargest(5).reset_index(name='Total_Flights')
print("Top 5 airlines by total flights:")
print(task8_airline)

"""**Task 9:** Quantile Aggregation

**Question:** Calculate the 90th percentile (P90)departure delay value for each airline carrier.
"""

#code
task9_airline = df_airline.groupby(airline_col)[dep_col].value_counts().unstack(fill_value=0)
print("Departure Time Distribution by Airline (Counts):")
print(task9_airline.head(5))

"""**Task 10:** Frequency Counting via Groupby

**Question:** Identify the Top 10 busiest Origin- to-Destination flight routes by total flight volume.
"""

#code
task10_airline = (df_airline.groupby([origin_col, dest_col])
                 .size()
                 .reset_index(name='Flight_Count')
                 .sort_values(by='Flight_Count', ascending=False)
                 .head(10))
print(task10_airline)

"""# DAY 6 & 7"""

#Loading Dataset
df_airline = pd.read_csv("/content/airlines_flights_data.csv")

print("Dataset loaded successfully")

"""**Task 1:** Calculate Mean Flight Price per Airline using transform()"""

#code
df_airline['Avg_Airline_Price'] = df_airline.groupby('airline')['price'].transform('mean')
display(df_airline.head())

"""**Task 2:** Calculate Price Deviation from Airline Average"""

#code
df_airline['Price_Difference'] = df_airline['price'] - df_airline['Avg_Airline_Price']
display(df_airline.head())

"""**Task 3:** Create Pivot Table of Average Price by Source City and Class"""

#code
pivot_table_avg_price = df_airline.pivot_table(index='source_city', columns='class', values='price', aggfunc='mean')
display(pivot_table_avg_price.head())

"""**Task 4:** Categorize Duration into Flight Distance Categories"""

#code
import numpy as np

bins = [0, 5, 15, np.inf]
labels = ['Short Haul', 'Medium Haul', 'Long Haul']

df_airline['Flight_Category'] = pd.cut(df_airline['duration'], bins=bins, labels=labels, right=False)

display(df_airline.head())

"""**Task 5:** Extract Flight Prefix Code using Vectorised String Operation."""

#code
df_airline['Flight_Prefix'] = df_airline['flight'].str.split('-').str[0]
display(df_airline.head())

"""**Task 6:** Filter Flights Departing in Early Morning with Price Above Median."""

#code
overall_median_price = df_airline['price'].median()

filtered_flights = df_airline[
    (df_airline['departure_time'].isin(['Early_Morning', 'Morning'])) &
    (df_airline['price'] > overall_median_price)
]

display(filtered_flights.head())

"""**Task 7:** Create a Route Column Combining Source and Destination Cities."""

#code
df_airline['Route'] = df_airline['source_city'] + ' -> ' + df_airline['destination_city']
display(df_airline.head())

"""**Task 8:** Calculate Ranking of Flights by PRice within Each Airline"""

#code
df_airline['Price_Rank_Within_Airline'] = df_airline.groupby('airline')['price'].rank(ascending=False, method='first')
display(df_airline.sort_values(by=['airline', 'Price_Rank_Within_Airline']).head())

"""**Task 9:** Cross-Tabulate Counts of Flights between SIrline and Stops."""

#code
crosstab_flights_stops = pd.crosstab(df_airline['airline'], df_airline['stops'])
display(crosstab_flights_stops)

"""**Task 10:** Identify Top 3 Longest Duration Flights for Each Airline"""

#code
top_3_longest_flights = df_airline.groupby('airline').apply(lambda x: x.nlargest(3, 'duration')).reset_index(drop=True)
display(top_3_longest_flights)

"""# DAY 8 & 9

**Task 1:** Plot Flight Price Distribution with KDE
"""

import seaborn as sns
import matplotlib.pyplot as plt

plt.figure(figsize=(10, 6))
sns.histplot(df_airline['price'], kde=True)
plt.title('Distribution of Flight Prices with KDE')
plt.xlabel('Price')
plt.ylabel('Frequency')
plt.show()

"""**Task 2:** Comparitive Box Plot of Price across Airlines."""

import seaborn as sns
import matplotlib.pyplot as plt

plt.figure(figsize=(12, 7))
sns.boxplot(x='airline', y='price', data=df_airline)
plt.title('Price Distribution Across Airlines')
plt.xlabel('Airline')
plt.ylabel('Price')
plt.xticks(rotation=45)
plt.grid(axis='y', linestyle='--', alpha=0.7)
plt.tight_layout()
plt.show()

"""**Task 3:** Violin Plot of Price by Ticket Class"""

import seaborn as sns
import matplotlib.pyplot as plt

plt.figure(figsize=(10, 6))
sns.violinplot(x='class', y='price', data=df_airline)
plt.title('Price Distribution Across Travel Classes')
plt.xlabel('Travel Class')
plt.ylabel('Price')
plt.grid(axis='y', linestyle='--', alpha=0.7)
plt.tight_layout()
plt.show()

"""**Task 4:** Flight Duration vs. Price Scatter Plot split by Class"""

import seaborn as sns
import matplotlib.pyplot as plt

plt.figure(figsize=(12, 7))
sns.scatterplot(x='duration', y='price', hue='class', data=df_airline, alpha=0.5)
plt.title('Flight Duration vs. Price by Class')
plt.xlabel('Duration (hours)')
plt.ylabel('Price')
plt.grid(True, linestyle='--', alpha=0.7)
plt.tight_layout()
plt.show()

"""**Task 5:** Heatmap Matrix of Mean Prices between Source and Destination"""

import seaborn as sns
import matplotlib.pyplot as plt

pivot_table_mean_price = df_airline.pivot_table(index='source_city', columns='destination_city', values='price', aggfunc='mean')

plt.figure(figsize=(10, 8))
sns.heatmap(pivot_table_mean_price, annot=True, fmt=".0f", cmap="YlGnBu", linewidths=.5)
plt.title('Mean Flight Price between Source and Destination Cities')
plt.xlabel('Destination City')
plt.ylabel('Source City')
plt.tight_layout()
plt.show()

"""**Task 6:** Average Price by Departure Time Bar PLot"""

import seaborn as sns
import matplotlib.pyplot as plt

plt.figure(figsize=(12, 7))
avg_price_by_departure = df_airline.groupby('departure_time')['price'].mean().reset_index()

plt.figure(figsize=(10, 6))
sns.barplot(x='departure_time', y='price', data=avg_price_by_departure, palette='viridis')
plt.title('Average Flight Price by Departure Time')
plt.xlabel('Departure Time')
plt.ylabel('Average Price')
plt.grid(axis='y', linestyle='--', alpha=0.7)
plt.tight_layout()
plt.show()

"""**Task 7:** Correlation Matrix of NUmerical FLight Attributes"""

import seaborn as sns
import matplotlib.pyplot as plt

# Create a mapping for the 'stops' column from string to numerical values
# Assuming 'stops' can take values like 'zero', 'one', 'two', etc.
stop_mapping = {
    'zero': 0,
    'one': 1,
    'two': 2,
    'three': 3,
    'four': 4,
    'five': 5
}

# Apply the mapping to create a new numerical column 'stops_numeric'
# Any 'stops' values not found in the mapping will become NaN.
df_airline['stops_numeric'] = df_airline['stops'].map(stop_mapping)

# Select the numerical columns for correlation calculation
# 'duration' and 'price' are already numeric. 'stops_numeric' is newly created.
numerical_cols = ['duration', 'price', 'stops_numeric']

# Compute the correlation matrix
# df.corr() will automatically handle NaN values by performing pairwise deletion.
correlation_matrix = df_airline[numerical_cols].corr()

# Plot the heatmap of the correlation matrix
plt.figure(figsize=(8, 6))
sns.heatmap(correlation_matrix, annot=True, cmap='coolwarm', fmt=".2f")
plt.title('Correlation Matrix of Flight Duration, Price, and Number of Stops')
plt.show()

"""**Task 8:** Horizontal Countplot of FLights per Airline"""

import seaborn as sns
import matplotlib.pyplot as plt

plt.figure(figsize=(10, 7))
sns.countplot(y='airline', data=df_airline, order=df_airline['airline'].value_counts().index, palette='viridis')
plt.title('Number of Flights per Airline')
plt.xlabel('Number of Flights')
plt.ylabel('Airline')
plt.grid(axis='x', linestyle='--', alpha=0.7)
plt.tight_layout()
plt.show()

"""**Task 9:** FacetGrid/Catplot Comparison across Stops"""

import seaborn as sns
import matplotlib.pyplot as plt
import pandas as pd

# Define the order for the 'stops' categories to ensure correct plotting order
stop_order = ['zero', 'one', 'two', 'three', 'four', 'five']

# Convert 'stops' column to an ordered categorical type
df_airline['stops'] = pd.Categorical(df_airline['stops'], categories=stop_order, ordered=True)

# Create the catplot to show flight prices across stops categories by travel class
g = sns.catplot(x='stops', y='price', col='class', data=df_airline, kind='box', height=5, aspect=1.2)

g.set_axis_labels("Number of Stops", "Flight Price")
g.set_titles("Travel Class: {col_name}")
plt.suptitle('Flight Prices Across Stops Categories by Travel Class', y=1.02) # Adjust suptitle position
plt.tight_layout(rect=[0, 0, 1, 0.98]) # Adjust layout to prevent title overlap
plt.show()

"""**Task 10:** 100-Flight Moving Average Price Trend Simulation"""

df_airline['rolling_mean_price'] = df_airline['price'].rolling(window=100).mean()
display(df_airline.head())

"""# DAY 10 AND 11"""

#Loading Dataset
df_airline = pd.read_csv("/content/airlines_flights_data.csv")

print("Dataset loaded successfully")

"""**Task 1:** Plotly Interactive Flight Price Histogram"""

import plotly.express as px

fig = px.histogram(df_airline, x='price', color='class',
                   title='Interactive Flight Price Distribution by Travel Class',
                   labels={'price': 'Flight Price', 'class': 'Travel Class'})
fig.update_layout(bargap=0.1)
fig.show()

"""**Task 2:** Interactive 3D Scatter (Duration vs. Price vs. Days left)"""

import plotly.express as px

fig = px.scatter_3d(df_airline, x='duration', y='price', z='days_left',
                    color='class',
                    symbol='stops',
                    hover_data=['airline', 'source_city', 'destination_city'],
                    title='Interactive 3D Scatter Plot: Duration vs. Price vs. Days Left by Airline',
                    labels={'duration': 'Flight Duration', 'price': 'Flight Price', 'days_left': 'Days Left'})

fig.show()

"""**Task 3:** Interactive Route Matrix Heatmap"""

import plotly.express as px

# Calculate the average price for each source-destination pair
avg_price_route = df_airline.groupby(['source_city', 'destination_city'])['price'].mean().reset_index()

# Create the interactive density heatmap
fig = px.density_heatmap(avg_price_route, x='source_city', y='destination_city', z='price',
                         title='Interactive Density Heatmap of Average Flight Price by Route',
                         labels={'source_city': 'Source City', 'destination_city': 'Destination City', 'price': 'Average Price'},
                         color_continuous_scale="Viridis")

fig.show()

"""**Task 4:** Parallel Categories Chart for Flight Attributes"""

import plotly.express as px

fig = px.parallel_categories(df_airline,
                             dimensions=['airline', 'source_city', 'class'],
                             color='price',
                             color_continuous_scale=px.colors.sequential.Viridis,
                             title='Parallel Categories Diagram of Flights by Airline, Source City, and Class')
fig.show()

"""**Task 5:** Animated Scatter Plot of Price vs Duration across Departure Times"""

import plotly.express as px

# Create an animated scatter plot of duration vs. price, animated by departure_time
fig = px.scatter(df_airline, x='duration', y='price', animation_frame='departure_time',
                   title='Animated Flight Duration vs. Price by Departure Time',
                   labels={'duration': 'Flight Duration (hours)', 'price': 'Flight Price', 'departure_time': 'Departure Time'},
                   size='price', # size of markers based on price for better visualization
                   color='class', # color points by travel class
                   hover_name='airline') # display airline name on hover

# Update animation speed if desired (e.g., 1000ms per frame)
fig.layout.updatemenus[0].buttons[0].args[1]['frame']['duration'] = 1000
fig.layout.updatemenus[0].buttons[0].args[1]['transition']['duration'] = 500

fig.show()

"""**Task 6:** Interactive Violin Plot of Flight Prices across Airlines"""

import plotly.express as px

fig = px.violin(df_airline, x='airline', y='price', color='class',
                   box=True, points='outliers',
                   title='Interactive Flight Price Distribution by Airline and Class',
                   labels={'airline': 'Airline', 'price': 'Flight Price', 'class': 'Travel Class'})
fig.update_layout(violinmode='group') # Ensure violins are grouped by class for each airline
fig.show()

"""**Task 7:** Hierarchical Airline Treemap"""

import plotly.express as px

fig = px.treemap(df_airline, path=['airline', 'source_city', 'destination_city'], values='price', color='price', hover_data=['airline', 'source_city', 'destination_city'], title='Hierarchical Treemap of Flights by Airline, Source City, Destination City (Scaled by Price)', color_continuous_scale='viridis')
fig.show()

"""**Task 8:** Interactive Polar/Radar Chart Simulation"""

import plotly.graph_objects as go

# Calculate mean price per airline
mean_price_per_airline = df_airline.groupby('airline')['price'].mean().reset_index()

# Create the Polar Radar chart
fig = go.Figure(
    data=go.Scatterpolar(
        r=mean_price_per_airline['price'],
        theta=mean_price_per_airline['airline'],
        fill='toself'
    ),
    layout=go.Layout(
        title=go.layout.Title(text='Mean Flight Price Across Airlines (Polar Radar Chart)'),
        polar=dict(
            radialaxis=dict(
                visible=True,
                range=[0, mean_price_per_airline['price'].max() * 1.1] # Extend range slightly above max for better visualization
            )
        ),
        showlegend=False
    )
)

fig.show()

"""**Task 9:** Custom Hover Template Scatter Plot"""

import plotly.express as px

fig = px.scatter(df_airline, x='duration', y='price', color='airline',
                 hover_name='flight', # Main name shown on hover
                 title='Flight Duration vs. Price with Custom Hover Details',
                 labels={'duration': 'Flight Duration (hours)', 'price': 'Flight Price'})

# Define the custom hover template
custom_hover_template = """
<b>Flight:</b> %{hover_name}<br>
<b>Airline:</b> %{customdata[0]}<br>
<b>Source:</b> %{customdata[1]}<br>
<b>Destination:</b> %{customdata[2]}<br>
<b>Class:</b> %{customdata[3]}<br>
<b>Duration:</b> %{x:.2f} hours<br>
<b>Price:</b> $%{y:,.2f}<extra></extra>"""

fig.update_traces(
    customdata=df_airline[['airline', 'source_city', 'destination_city', 'class']],
    hovertemplate=custom_hover_template
)

fig.show()

"""**Task 10:** Multi-Panel Interactive Subplots Container"""

import plotly.graph_objects as go
from plotly.subplots import make_subplots

# Calculate mean price per airline for the bar chart
mean_price_per_airline = df_airline.groupby('airline')['price'].mean().reset_index()

# Create subplots
fig = make_subplots(rows=1, cols=2,
                    subplot_titles=('Average Price per Airline', 'Flight Duration vs. Price'))

# Add Bar chart (Average Price per Airline)
fig.add_trace(go.Bar(x=mean_price_per_airline['airline'],
                     y=mean_price_per_airline['price'],
                     name='Avg Price'),
              row=1, col=1)

# Add Scatter plot (Flight Duration vs. Price)
fig.add_trace(go.Scatter(x=df_airline['duration'],
                         y=df_airline['price'],
                         mode='markers',
                         marker=dict(color=df_airline['price'], colorscale='Viridis', showscale=True),
                         name='Flight Data'),
              row=1, col=2)

# Update layout
fig.update_layout(title_text='Multi-Panel Flight Analysis', height=500, showlegend=False)
fig.update_xaxes(title_text='Airline', row=1, col=1)
fig.update_yaxes(title_text='Average Price', row=1, col=1)
fig.update_xaxes(title_text='Duration (hours)', row=1, col=2)
fig.update_yaxes(title_text='Price', row=1, col=2)

fig.show()

"""# DAY 12 & 13"""

#Import libraries

!pip install squarify
!pip install ydata_profiling
!pip install wordcloud
!pip install dash

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import missingno as msno
import squarify
from wordcloud import WordCloud
from ydata_profiling import ProfileReport

import dash
from dash import dcc, html
from dash.dependencies import Input, Output
import plotly.express as px
import plotly.graph_objects as go

import kagglehub
import os

# 2. Download and load real Airline Flights Dataset
path = kagglehub.dataset_download("rohitgrewal/airlines-flights-data")
df_flights = pd.read_csv(os.path.join(path, "airlines_flights_data.csv"))
df_flights_sub = df_flights.sample(n=500, random_state=42).reset_index(drop=True)

print("Airline Flights sample shape:", df_flights_sub.shape)

"""**Task 4:** Textual Category Density Word Cloud"""

from wordcloud import WordCloud
import matplotlib.pyplot as plt

# Combine airline and destination city to create route strings
df_airline['route_description'] = df_airline['airline'] + ' to ' + df_airline['destination_city']

# Join all route descriptions into a single string
all_routes_text = ' '.join(df_airline['route_description'].astype(str))

# Generate a word cloud
wordcloud = WordCloud(width=800, height=400, background_color='white').generate(all_routes_text)

# Display the generated image:
plt.figure(figsize=(12, 6))
plt.imshow(wordcloud, interpolation='bilinear')
plt.axis('off')
plt.title('Route Frequency Density Word Cloud (Airline & Destination City)')
plt.show()